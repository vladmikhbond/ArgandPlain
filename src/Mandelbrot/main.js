const canvas = document.getElementById("canvas");

const CANVAS_R = canvas.width / 2; 
const AREA = new Area(0, 0, 2);
const EXPRS = [null, null, null];
let MAX_LEVEL = 100;
let ENLAG = 4;

setTimeout(refresh, 10);



